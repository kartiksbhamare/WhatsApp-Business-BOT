# ==================================================
# Smart WhatsApp Booking Bot - Environment Configuration
# ==================================================

# Service URLs - Configure these based on your deployment environment
# For local development: use localhost URLs
# For Railway/production: use your deployed URLs

# Backend API Configuration
BACKEND_URL=http://localhost:8000
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# WhatsApp Service Configuration  
WHATSAPP_SERVICE_URL=http://localhost:3000
WHATSAPP_HOST=0.0.0.0
WHATSAPP_PORT=3000

# Salon Configuration
SALON_NAME=Beauty Salon

# Firebase Configuration
FIREBASE_PROJECT_ID=appointment-booking-4c50f
FIREBASE_CREDENTIALS_PATH=firebase-key.json
# For Railway deployment, use base64 encoded credentials:
# FIREBASE_CREDENTIALS_BASE64=your_base64_encoded_credentials_here

# Google Calendar Integration (Optional)
GOOGLE_CALENDAR_CREDENTIALS_PATH=client_secret.json
GOOGLE_CALENDAR_TOKEN_PATH=token.json
GOOGLE_CALENDAR_ID=

# Application Settings
DEBUG=false
LOG_LEVEL=INFO

# Chrome/Puppeteer Configuration (for containers)
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
PUPPETEER_ARGS=--no-sandbox --disable-setuid-sandbox --disable-dev-shm-usage

# Railway/Container Environment Detection
RAILWAY_ENVIRONMENT=
DOCKER_ENV=

# ==================================================
# Deployment Examples:
# ==================================================

# Local Development:
# BACKEND_URL=http://localhost:8000
# WHATSAPP_SERVICE_URL=http://localhost:3000

# Railway Production:
# BACKEND_URL=https://your-backend-app.railway.app
# WHATSAPP_SERVICE_URL=https://your-whatsapp-app.railway.app

# Docker Compose:
# BACKEND_URL=http://backend:8000
# WHATSAPP_SERVICE_URL=http://whatsapp:3000 